apiVersion: v1
kind: ConfigMap
metadata:
  name: eshop-config
  namespace: default
data:
  DATABASE_HOST: "postgres-service"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "eshop"
  DATABASE_USER: "postgres"
  NODE_ENV: "production"
