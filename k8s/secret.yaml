apiVersion: v1
kind: Secret
metadata:
  name: eshop-secret
  namespace: default
type: Opaque
stringData:
  DATABASE_PASSWORD: "REPLACE_WITH_REAL_PASSWORD"
  DATABASE_URL: "postgresql://postgres:REPLACE_WITH_REAL_PASSWORD@postgres-service:5432/eshop"
